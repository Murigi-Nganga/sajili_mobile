import 'package:flutter/material.dart';

class LecScheduleScreen extends StatelessWidget {
  const LecScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Edit Schedule Screen'),
      ),
    );
  }
}
